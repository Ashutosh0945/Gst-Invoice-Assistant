# Console website (this folder). On Vercel, set these in Settings -> Environment Variables.
BACKEND_URL=http://localhost:8000        # your Render API address online
CONSOLE_API_KEY=change-me-console-server-key   # must equal API_KEY on the API server
NEXT_PUBLIC_BRAND_NAME=GST Desk
NEXT_PUBLIC_BRAND_ACCENT=#7C7CF2
